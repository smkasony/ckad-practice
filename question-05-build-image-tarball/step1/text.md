## Task

On the node, directory `/root/app-source` contains a valid `Dockerfile`.

### Your tasks
1. Build a container image with name `my-app:1.0` using `/root/app-source` as build context
2. Save the image as a tarball to `/root/my-app.tar`

Notes:
- Use Podman if available.
- Do not change the image name/tag or the tarball path.
