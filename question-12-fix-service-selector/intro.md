A Deployment named `web-app` exists in namespace `default`.

A Service named `web-svc` also exists, but its selector is incorrect.

In the next step you will fix the Service selector so it targets the web-app Pods.
