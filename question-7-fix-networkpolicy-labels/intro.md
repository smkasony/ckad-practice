This scenario uses a namespace `network-demo` with three Pods and three NetworkPolicies.

The NetworkPolicies expect specific `role` labels, but the Pods currently have incorrect labels.

Your job is to fix the Pod labels (without modifying any NetworkPolicy).
