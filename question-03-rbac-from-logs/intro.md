A namespace `audit` contains a Pod `log-collector` that is failing with authorization errors.

Your job is to read the Pod logs, determine the missing permissions, and fix it using RBAC.

Proceed to the next step when ready.
