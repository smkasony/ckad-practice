In this scenario you will create a Kubernetes `CronJob` in the `default` namespace.

The environment is reset automatically when the scenario starts.

Proceed to the next step to see the task requirements.
